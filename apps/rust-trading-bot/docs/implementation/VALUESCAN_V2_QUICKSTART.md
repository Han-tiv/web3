# 🚀 Valuescan V2 快速启动指南

**创建日期**: 2025-11-21
**状态**: ✅ 生产就绪

---

## 📋 前置条件检查

```bash
cd /home/hanins/code/web3/apps/rust-trading-bot

# 1. 检查编译状态
cargo build --bin integrated_ai_trader --release

# 2. 检查环境变量 (根目录 .env)
grep "BINANCE_API_KEY\|GEMINI_API_KEY\|TELEGRAM" /home/hanins/code/web3/.env
```

---

## ⚡ 一键启动 (推荐)

### 启动 V2 版本 (Valuescan 关键位交易法)

```bash
# 方式1: 使用启动脚本 (推荐)
bash start_trader_v2.sh v2

# 方式2: 手动启动
export USE_VALUESCAN_V2=true
cargo run --bin integrated_ai_trader --release > trader.log 2>&1 &
```

### 启动 V1 版本 (原版K线形态)

```bash
bash start_trader_v2.sh v1
```

---

## 🔍 实时监控

```bash
# 查看实时日志
tail -f trader.log

# 检查 Valuescan 版本
grep "Valuescan版本" trader.log

# 查看 V2 评分信息
grep "V2评分" trader.log

# 查看 V2 关键位信息
grep "V2关键位" trader.log
```

---

## 🛑 停止程序

```bash
# 使用停止脚本 (推荐)
bash stop_trader.sh

# 或手动停止
kill $(cat trader.pid)
```

---

## 📊 V2 核心特性

### 开仓决策 (Valuescan 关键位交易法)

**评分系统**: 0-10分, ≥6分才开仓

**权重分配**:
- 关键位判断: **50%** ⭐⭐⭐⭐⭐ (核心)
- 资金流向: **30%** ⭐⭐⭐
- 技术指标: **20%** ⭐⭐

**开仓检查清单**: 10项检查, 8项满足才开仓
1. ✓ 距关键位>3%?
2. ✓ 突破且量>1.5倍?
3. ✓ 资金与价格一致?
4. ✓ 止损≤5%?
5. ✓ 风险收益比≥2:1?
6. ✓ 单笔风险≤5%?
7. ✓ 无FOMO/恐慌?
8. ✓ 避开整数关口?
9. ✓ 空间>3-5%?
10. ✓ 最大亏损可承受?

### 持仓管理 (关键位止盈法)

**决策优先级**:
1. **关键位止盈 (60%)** - 最高优先级
2. **K线反转信号 (30%)** - 次优先级
3. **盈利时间参考 (10%)** - 灵活建议

**代码自动止损** (AI无需判断):
- 持仓>4h且盈利<1% → 自动全平
- 亏损>-5% → 自动全平
- 跌破Level 3支撑 → 自动全平

---

## ✅ V2 生效验证

### 1. 检查日志中的版本信息

```bash
grep "Valuescan版本" trader.log
```

**期望输出**:
```
🤖 Valuescan版本: V2 (USE_VALUESCAN_V2=true)
```

### 2. 检查评分系统

```bash
grep "V2评分" trader.log | tail -5
```

**期望输出**:
```
🏅 Valuescan V2评分: 8.5/10 | 风险收益比: 2.5 | 仓位建议: 25.0%
   V2关键位: 阻力=$3.30 | 支撑=$3.06 | 位置=刚突破阻力,距下一阻力5.8%
```

### 3. 检查开仓决策

```bash
grep "AI决策" trader.log | tail -10
```

**V2特征**: 包含详细的关键位、评分、风险收益比信息

---

## 🐛 常见问题排查

### 问题1: V2评分信息没有出现

**检查**:
```bash
echo $USE_VALUESCAN_V2
```

**解决**:
```bash
# 重新启动,明确指定V2
bash start_trader_v2.sh v2
```

### 问题2: 编译失败

**检查**:
```bash
ls -l src/valuescan_v2.rs
grep "valuescan_v2" src/lib.rs
```

**解决**:
```bash
cargo clean
cargo build --bin integrated_ai_trader --release
```

### 问题3: Gemini API 超时

**现象**: 日志显示 "AI开仓分析超时180s (V2)"

**原因**: Gemini API 响应慢或网络问题

**解决**:
1. 检查网络连接
2. 确认 GEMINI_API_KEY 配置正确
3. 观察是否持续超时 (可能需要更换 API key)

---

## 📈 测试建议

### 阶段1: 小资金测试 (1-3天)

```bash
# 启动V2
bash start_trader_v2.sh v2

# 实时监控
tail -f trader.log | grep -E "V2评分|V2关键位|AI决策"
```

**建议**:
- 仅测试 1-2 个交易对
- 每笔交易限制在总资金的 5% 以内
- 详细记录每笔交易的评分和结果

### 阶段2: 对比测试 (1周)

启动两个实例对比:
```bash
# 终端1: V1版本 (测试账户1)
bash start_trader_v2.sh v1

# 终端2: V2版本 (测试账户2)
bash start_trader_v2.sh v2
```

**记录**:
- 开仓次数对比
- 成功率对比
- 盈亏对比
- 评分系统准确性

### 阶段3: 全面部署 (确认有效后)

```bash
# 正式环境启动V2
bash start_trader_v2.sh v2

# 监控关键指标
watch -n 10 'tail -20 trader.log | grep -E "V2评分|持仓检查|试探建仓"'
```

---

## 📊 性能监控命令

```bash
# 开仓成功率
grep "✅ 试探建仓订单" trader.log | wc -l

# V2评分分布
grep "V2评分" trader.log | awk '{print $4}' | sort -n

# 平均风险收益比
grep "风险收益比" trader.log | awk '{print $7}' | awk -F'|' '{sum+=$1; count++} END {print sum/count}'

# 当前持仓数量
grep "📊.*持仓检查" trader.log | tail -1
```

---

## 🔗 相关文档

| 文档 | 说明 |
|------|------|
| `AI_PROMPTS_V2.md` | V2 prompt 完整规范 |
| `VALUESCAN_V2_IMPLEMENTATION.md` | 技术实施文档 |
| `VALUESCAN_V2_USAGE_GUIDE.md` | 详细使用指南 |
| `VALUESCAN_V2_COMPLETE.md` | 完整实施报告 |

---

## ⚠️ 重要提醒

1. **V2 未经实盘验证**: 强烈建议小资金测试
2. **环境变量检查**: 确保根目录 `.env` 配置正确
3. **API 权限**: 确认 Binance API 已开启 Futures 权限
4. **实时监控**: 测试期间密切关注日志输出
5. **风险控制**: 遵循单笔 5% 风险限制

---

## 🎯 下一步

**立即开始测试**:

```bash
cd /home/hanins/code/web3/apps/rust-trading-bot
bash start_trader_v2.sh v2
tail -f trader.log
```

**祝交易顺利! 🚀**

---

**最后更新**: 2025-11-21
**版本**: V2.0
**状态**: ✅ 生产就绪
