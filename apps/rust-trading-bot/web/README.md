# AI交易机器人Web监控面板

## 📊 功能特性

- ✅ **实时权益曲线**: 展示账户权益变化,支持美元/百分比切换
- ✅ **当前持仓列表**: 实时显示所有持仓及盈亏情况
- ✅ **交易历史记录**: 查看历史交易及统计数据
- ✅ **手动平仓功能**: 支持一键平仓
- ✅ **自动刷新**: 数据定时自动更新(5-30秒)
- ✅ **Binance深色主题**: 专业的币安风格UI

## 🚀 快速开始

### 前端开发服务器

```bash
cd /home/hanins/code/web3/apps/rust-trading-bot/web

# 安装依赖(已完成)
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build
```

访问地址: http://localhost:5174 (或 http://localhost:5173)

### 当前状态

- ✅ 前端已创建完成并启动
- ✅ Rust后端API已实现并集成
- ✅ Web服务器在端口8080运行
- ✅ 所有API端点正常工作

### 部署状态

- 后端服务器地址: http://localhost:8080
- 前端开发服务器: http://localhost:5174 (或 http://localhost:5173)
- API测试脚本: `test_api.sh`
- 数据更新机制已集成到交易机器人中

## 📁 项目结构

```
web/
├── src/
│   ├── components/
│   │   ├── EquityChart.tsx      # 权益曲线图
│   │   ├── PositionsList.tsx    # 持仓列表
│   │   └── TradesHistory.tsx    # 交易历史
│   ├── lib/
│   │   ├── api.ts               # API封装
│   │   └── mockApi.ts           # Mock数据(开发用)
│   ├── types/
│   │   └── index.ts             # TypeScript类型定义
│   ├── App.tsx                  # 主应用
│   ├── main.tsx                 # 入口文件
│   └── index.css                # 全局样式
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

## 🔌 Rust API端点

### 1. GET /api/account - 账户摘要

```json
{
  "total_equity": 1050.25,
  "available_balance": 950.25,
  "unrealized_pnl": 50.25,
  "initial_balance": 1000.0,
  "total_trades": 15,
  "win_rate": 0.67
}
```

### 2. GET /api/equity-history - 权益历史

```json
[
  {
    "timestamp": "2024-11-08T10:00:00Z",
    "total_equity": 1050.25,
    "pnl": 50.25,
    "pnl_pct": 5.025
  }
]
```

### 3. GET /api/positions - 当前持仓

```json
[
  {
    "symbol": "BTCUSDT",
    "side": "LONG",
    "entry_price": 43250.5,
    "current_price": 43580.2,
    "quantity": 0.023,
    "pnl": 7.58,
    "pnl_pct": 0.76,
    "entry_time": "2024-11-08T08:00:00Z",
    "leverage": 5
  }
]
```

### 4. GET /api/trades?limit=50 - 交易历史

```json
[
  {
    "id": "1",
    "symbol": "SOLUSDT",
    "side": "LONG",
    "entry_price": 95.2,
    "exit_price": 98.5,
    "quantity": 10,
    "pnl": 33.0,
    "pnl_pct": 3.47,
    "entry_time": "2024-11-08T05:00:00Z",
    "exit_time": "2024-11-08T07:00:00Z",
    "hold_duration": 7200
  }
]
```

### 5. POST /api/positions/{symbol}/close - 手动平仓

无请求体,成功返回200 OK。

## 📝 使用说明

### 开发模式

1. 启动Rust交易机器人(后端),交易机器人会自动启动Web API并监听 http://localhost:8080
2. (可选) 启动前端开发服务器: `npm run dev`
3. 浏览器访问: http://localhost:5174 (或 http://localhost:5173)
4. 前端会自动代理 `/api` 请求到 `http://localhost:8080`

### 生产部署

1. 构建前端: `npm run build`
2. 将 `dist/` 目录静态文件托管到Nginx或Axum
3. 配置Nginx反向代理API请求

## 🎨 技术栈

- **前端**: React 18 + TypeScript + Vite
- **UI框架**: Tailwind CSS
- **图表**: Recharts
- **数据获取**: SWR (自动缓存和刷新)
- **图标**: lucide-react
- **后端**: Rust + Axum

## 🔧 常用命令

```bash
# 启动前端开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 查看Vite日志
tail -f vite.log

# 停止Vite服务器
kill $(cat vite.pid)  # 如果保存了PID
# 或者
ps aux | grep vite | grep -v grep | awk '{print $2}' | xargs kill
```

## 📊 效果预览

访问 http://localhost:5173 可以看到:

1. **顶部导航**: 显示标题和GitHub链接
2. **权益曲线图**:
   - 美元/百分比切换
   - 统计卡片(当前权益、盈亏、初始余额)
   - 交互式图表
3. **持仓列表**:
   - 交易对、方向、价格、盈亏
   - 支持一键平仓
4. **交易历史**:
   - 历史交易记录
   - 盈亏统计
   - 持仓时长

## ⚠️ 注意事项

- 面板实时数据由交易机器人提供,确保交易所API密钥与风控配置正确
- 保持交易机器人进程运行即可让Web API与数据更新持续可用
- 生产环境建议添加身份认证
- 建议使用HTTPS保护数据传输

---

**当前状态**: Web服务器与交易机器人已集成,可直接对接真实交易数据
