# 🚀 AI交易机器人 - 快速开始指南

## 📋 系统概述

这是一个基于Rust的AI交易机器人，集成了Web监控面板，支持Binance期货交易（经典账户FAPI）。

### 核心功能

- 🤖 **AI智能交易**: 基于DeepSeek AI的决策引擎
- 📊 **实时监控**: Web界面实时显示账户、持仓、交易历史
- 🔄 **自动化管理**: 自动止损、止盈、仓位管理
- 📱 **Telegram集成**: 信号监听和通知推送
- 🌐 **Web API**: RESTful API接口

## 🎯 快速启动

### 一键启动

```bash
cd /home/hanins/code/web3/apps/rust-trading-bot
./start.sh
```

### 一键停止

```bash
./stop.sh
```

### 系统诊断

```bash
./system_check.sh
```

### API测试

```bash
./test_api.sh
```

## 📡 服务地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端监控面板 | http://localhost:5173 | 权益曲线、持仓、交易历史 |
| Web API | http://localhost:8080/api/ | RESTful API接口 |
| 健康检查 | http://localhost:8080/health | 系统状态检查 |

## 🛠️ 配置说明

### 环境变量

配置文件: `.env`

```bash
# Binance API配置
BINANCE_API_KEY=your_api_key
BINANCE_SECRET=your_secret_key
BINANCE_TESTNET=false

# AI配置
DEEPSEEK_API_KEY=your_deepseek_key

# Telegram配置
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
```

### API密钥配置步骤

1. **登录Binance**
   - 访问 https://www.binance.com
   - 登录你的账户

2. **创建API密钥**
   - 用户中心 → API管理 → 创建API

3. **配置权限**
   - ☑ 读取权限 (Read)
   - ☑ 合约交易权限 (Futures Trading)
   - ☑ 交易权限 (Trade)

4. **IP白名单**
   - 添加服务器IP: `23.27.11.181`
   - 或选择"不限制"（不推荐）

5. **更新.env文件**
   ```bash
   nano .env
   # 修改 BINANCE_API_KEY 和 BINANCE_SECRET
   ```

6. **重启系统**
   ```bash
   ./stop.sh
   ./start.sh
   ```

## 📊 Web监控面板

### 功能特性

1. **权益曲线图**
   - 实时显示账户权益变化
   - 支持美元/百分比切换
   - 30秒自动刷新

2. **持仓列表**
   - 显示所有持仓及盈亏
   - 支持手动平仓
   - 5秒自动刷新

3. **交易历史**
   - 历史交易记录
   - 盈亏统计
   - 持仓时长

### 访问方式

浏览器打开: http://localhost:5173

## 🔧 API端点

### GET /api/account
获取账户信息

```bash
curl http://localhost:8080/api/account
```

响应:
```json
{
  "total_equity": 1000.0,
  "available_balance": 1000.0,
  "unrealized_pnl": 0.0,
  "initial_balance": 1000.0,
  "total_trades": 0,
  "win_rate": 0.0
}
```

### GET /api/equity-history
获取权益历史

```bash
curl http://localhost:8080/api/equity-history
```

### GET /api/positions
获取当前持仓

```bash
curl http://localhost:8080/api/positions
```

### GET /api/trades
获取交易历史

```bash
curl http://localhost:8080/api/trades?limit=50
```

### POST /api/positions/:symbol/close
手动平仓

```bash
curl -X POST http://localhost:8080/api/positions/BTCUSDT/close
```

## 📝 日志管理

### 日志位置

```
logs/
├── trader_20251109_094325.log   # 交易机器人日志
└── vite.log                      # 前端服务日志
```

### 查看实时日志

```bash
# 交易机器人日志
tail -f logs/trader_*.log

# 前端服务日志
tail -f logs/vite.log
```

### 日志轮转

建议配置logrotate自动管理日志文件大小。

## 🔍 故障排查

### 常见问题

#### 1. API认证失败 (-2015)

**错误信息**:
```
Invalid API-key, IP, or permissions for action
```

**解决方案**:
1. 检查API密钥是否正确
2. 添加服务器IP到白名单 (23.27.11.181)
3. 确认API权限启用了"合约交易"

#### 2. 前端无法访问

**检查端口**:
```bash
ss -tlnp | grep 5173
```

**重启前端**:
```bash
./stop.sh
./start.sh
```

#### 3. Web API无响应

**检查进程**:
```bash
ps aux | grep integrated_ai_trader
```

**检查端口**:
```bash
ss -tlnp | grep 8080
```

#### 4. 编译失败

**清理并重新编译**:
```bash
cargo clean
cargo build --release --bin integrated_ai_trader
```

### 诊断工具

运行系统诊断脚本获取详细信息:

```bash
./system_check.sh
```

输出包括:
- ✅ 环境配置检查
- ✅ 进程状态检查
- ✅ 端口占用检查
- ✅ API连通性测试
- ✅ 日志分析
- ✅ 解决方案建议

## 📚 文档索引

| 文档 | 说明 |
|------|------|
| `README.md` | 本文档 - 快速开始指南 |
| `FAPI_MIGRATION.md` | Binance API迁移报告（统一账户→经典账户） |
| `WEB_INTEGRATION.md` | Web监控系统集成报告 |
| `SYSTEM_ANALYSIS.md` | 系统全面分析与优化报告 |
| `web/README.md` | 前端开发文档 |

## 🔄 开发工作流

### 编译

```bash
# Debug模式
cargo build --bin integrated_ai_trader

# Release模式（推荐）
cargo build --release --bin integrated_ai_trader
```

### 测试

```bash
# 运行测试
cargo test

# 运行特定测试
cargo test binance_client
```

### 代码检查

```bash
# 格式化
cargo fmt

# Lint检查
cargo clippy
```

## 🚀 部署建议

### 生产环境

1. **使用systemd管理**
   - 自动重启
   - 日志管理
   - 资源限制

2. **前端生产构建**
   ```bash
   cd web
   npm run build
   ```

3. **使用Nginx**
   - 反向代理API
   - 托管静态文件
   - SSL/TLS加密

4. **监控告警**
   - 配置Prometheus + Grafana
   - Telegram通知
   - 日志聚合

### 安全建议

- ✅ 使用IP白名单限制API访问
- ✅ 启用API权限最小化原则
- ✅ 定期轮换API密钥
- ✅ 使用HTTPS访问Web面板
- ✅ 设置合理的止损位
- ✅ 小额资金测试策略

## 📞 支持

### 问题反馈

如遇到问题，请提供:
1. `system_check.sh` 输出
2. 错误日志（最近100行）
3. 操作步骤描述

### 有用的命令

```bash
# 查看系统状态
./system_check.sh

# 测试API
./test_api.sh

# 查看进程
ps aux | grep -E "(integrated|vite)"

# 查看端口
ss -tlnp | grep -E "(5173|8080)"

# 查看日志
tail -100 logs/trader_*.log

# 重启系统
./stop.sh && ./start.sh
```

## 🎯 系统架构

```
┌─────────────────────────┐
│   Telegram信号源        │
│   (Valuescan频道)       │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Integrated AI Trader   │
│  ┌───────────────────┐  │
│  │ Signal Processing │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ AI Decision       │  │
│  │ (DeepSeek)        │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ Position Manager  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ Web State Manager │  │
│  └───────────────────┘  │
└─────┬─────────────┬─────┘
      │             │
      ▼             ▼
┌──────────┐  ┌─────────────┐
│ Binance  │  │  Web API    │
│ API      │  │  :8080      │
│ (FAPI)   │  └──────┬──────┘
└──────────┘         │
                     ▼
              ┌──────────────┐
              │ React前端    │
              │ :5173        │
              └──────────────┘
```

## ✨ 最近更新

### v0.1.0 (2025-11-09)

- ✅ 完成Binance API迁移（统一账户→经典账户）
- ✅ 集成Web监控系统（前端+后端）
- ✅ 创建启动/停止脚本
- ✅ 添加系统诊断工具
- ✅ 优化日志管理
- ✅ 完善文档

## 📄 许可证

MIT License

---

**享受智能交易！** 🚀📈

如有问题，请运行 `./system_check.sh` 获取诊断信息。
